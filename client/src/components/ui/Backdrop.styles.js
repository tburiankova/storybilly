import styled from 'styled-components';

export const BackdropDiv = styled.div`
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100vh;
  background: var(--whiteTrans);
  z-index: 99;
`;
